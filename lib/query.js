const R = require('ramda')

const config = require('../config')

module.exports = (models) => {
  return {
    find: (table, query) => {
    },
    findOne: (table, query) => {
    },
    create: (table, data) => {
    },
    update: (table, params, data) => {
    },
    delete: (table, params) => {
    },
    count: (table, params) => {
    },
  }
}
